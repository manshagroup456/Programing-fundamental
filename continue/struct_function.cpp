#include<iostream>
#include<conio.h>
#include<math.h>
using namespace std;
int main()
struct
{
    int id;
    int marks;
}
void getvalue(record *id);
void show(record *id );
main(
record r[5];
getvalue(r);
show(r);
)
void getvalue(record*id)
{
for(int i = 0 ; i<5; i++){

    cin>>id[i].id;
    cin>>id[i].marks;
}
}